# Copy this to .env.prod on the EC2 box and fill in real values. Never
# commit .env.prod itself (it's gitignored) — this .example file is the
# only one that belongs in git.

DOCKERHUB_USERNAME=changeme
DOMAIN=changeme.example.com
POSTGRES_USER=epfo
POSTGRES_PASSWORD=changeme-generate-a-real-one
POSTGRES_DB=epfo_one
OPENAI_API_KEY=
